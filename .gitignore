.env
.env.*
!.env.example
node_modules
dist/
coverage/

# Local SQLite database and its WAL/SHM sidecars
data/
*.sqlite
*.sqlite-wal
*.sqlite-shm

# Editors
.idea
.vscode
*.iml
*.swp

# Local agent and shell config that lives in this working tree but is not the
# project's. An unscoped `git add -A` swept these in once; ignoring them means it
# cannot happen again. `develop` carries none of them.
.claude/
.mcp.json
.bash_profile
.bashrc
.profile
.zprofile
.zshrc
.gitconfig
.ripgreprc
