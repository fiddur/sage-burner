{
  "name": "@sage-burner/shared",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "sideEffects": false,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "check": "tsgo --noEmit && oxlint -c ../../.oxlintrc.json --type-aware",
    "fix": "oxfmt . && oxlint -c ../../.oxlintrc.json --fix"
  },
  "dependencies": {
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "vitest": "^5"
  }
}
